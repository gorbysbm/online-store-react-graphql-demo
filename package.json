{
  "name": "sick-fits",
  "version": "1.0.0",
  "description": "run servers",
  "scripts": {
    "backend": "cd webstore/backend && npm install && npm start",
    "frontend": "cd webstore/frontend && npm install && npm run dev",
    "demo": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "dependencies": {
    "npm-run-all": "^4.1.3",
    "concurrently": "^5.1.0"
  },
  "license": "MIT",
  "repository": {
    "url": "wesbos.com"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "devDependencies": {}
}
